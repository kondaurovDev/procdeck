{
  "name": "example",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "pnpm --filter procdeck build && procdeck up --fg procdeck.config.ts"
  },
  "dependencies": {
    "procdeck": "workspace:*"
  }
}
